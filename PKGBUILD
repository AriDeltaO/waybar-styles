# Maintainer: AriDeltaO
pkgname=waybar-styles
pkgver=1.0.0
pkgrel=1
pkgdesc="Multiple themes and styles for Waybar"
arch=('any')
url="https://github.com/yourusername/waybar-styles"
license=('MIT')
depends=('waybar' 'nerd-fonts-complete' 'git')
source=("https://github.com/yourusername/waybar-styles/archive/v${pkgver}.tar.gz")
sha256sums=('SKIP')  # Generate this after the first build

# Package installation
package() {
    install -Dm755 "${srcdir}/waybar-styles-${pkgver}/themes" "${pkg

